reproducible research project 1
test 110220
first R Markdown file

load data

```{r}
library(datasets)
data(airquality)
summary(airquality)
```

make a pairs plot

```{r}
pairs(airquality)
```
